# CUSTOM-CONF: File automatically created/updated [custom-conf-php5-fpm]
# Depends: fastcgi actions

# PHP5-FPM has no specific extension to load
# (it depends on the FastCGI extension; see php5-fpm.conf)

