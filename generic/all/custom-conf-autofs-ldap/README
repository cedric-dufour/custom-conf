Custom Configuration Framework (custom-conf)
============================================
Package: custom-conf-autofs-ldap

This package provides standard configuration of AutoFS, the auto-mounting
daemon, along the Lightweight Directory Access Protocol (LDAP).

In particular, it addresses standard configuration of:
 - LDAP integration (<-> 'custom-conf-nss' package)

